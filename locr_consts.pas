unit locr_consts;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils;
resourcestring
  LOCR_Version='0.0.1';
  rsPage = 'Page';
  rsErrorGetting = 'Error getting image';
  rsErrorRecogni = 'Error recognizing file';
  rsNothingSelec = 'Nothing selected';
  rsNoWorkingIma = 'No working image found on the page';
implementation

end.

